---
import Logo from "./logo.astro";
import Item from "./item.astro";

const { href } = Astro.url;
---

<nav
  id="main_nav"
  class="flex items-center justify-between px-10 sticky top-[0] bg-[white] gap-3 h-[130px] w-full z-20"
>
  <section>
    <Logo aClassName="Nav-Item" viewId="top" />
  </section>
  <ul class="flex gap-5 items-center justify-between">
    <Item viewId="about" name="Nosotros" href={href} className="Nav-Item" />
    <Item viewId="services" name="Servicios" href={href} className="Nav-Item" />
    <Item viewId="projects" name="Proyectos" href={href} className="Nav-Item" />
    <Item viewId="footer" name="Contacto" href={href} className="Nav-Item" />
  </ul>
</nav>

<script>
  let isPrimary = false;
  const OFFSET_Y = 130;
  const Nav = document.querySelector("#main_nav") as HTMLDivElement;
  if (window.scrollY < OFFSET_Y) {
    Nav.style.background = "transparent";
    Nav.style.color = "white";
  }
  function onScroll() {
    if (window.scrollY < OFFSET_Y && isPrimary) {
      Nav.style.background = "transparent";
      Nav.style.color = "white";
      isPrimary = false;
    } else if (!isPrimary && window.scrollY > OFFSET_Y) {
      Nav.style.background = "white";
      Nav.style.color = "black";
      isPrimary = true;
    }
  }

  window.addEventListener("scroll", onScroll);

  function goTo(e: Event, element: HTMLAnchorElement) {
    e.preventDefault();
    const viewId = element.getAttribute("data-view-id");
    if (!viewId) return;
    if (viewId === "top") {
      window.scrollTo({ top: 0, behavior: "smooth" });
      return;
    }
    const view = document.querySelector(`#${viewId}`) as HTMLDListElement;
    view?.scrollIntoView({ behavior: "smooth" });
  }

  const NavItems = document.querySelectorAll(
    ".Nav-Item"
  ) as NodeListOf<HTMLAnchorElement>;

  NavItems.forEach((item) => {
    item.addEventListener("click", (e) => goTo(e, item));
  });
</script>
